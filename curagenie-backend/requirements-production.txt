# Core FastAPI Dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0
python-multipart==0.0.6

# Database
sqlalchemy==2.0.23
alembic==1.13.1

# Environment & Configuration
python-dotenv==1.0.0

# Web & HTTP
requests==2.31.0
websockets==12.0

# Basic Data Processing (lightweight versions)
pandas==2.1.4
numpy==1.25.2

# Optional: Include only if needed
# psycopg2-binary==2.9.9  # Only if using PostgreSQL
# redis==5.0.1  # Only if using Redis
# celery==5.3.4  # Only if using Celery
